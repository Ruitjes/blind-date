apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -o ./k8s
    kompose.version: 1.26.1 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: upload-service
  name: upload-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: upload-service
  strategy: {}
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -o ./k8s
        kompose.version: 1.26.1 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.service: upload-service
    spec:
      containers:
        - env:
            - name: AmazonS3Settings__AccessKey
            - name: AmazonS3Settings__BucketName
            - name: AmazonS3Settings__SecretKey
          image: seethrough2022/uploadservice:1.0.0
          imagePullPolicy: Always
          name: upload-service
          resources: {}
      restartPolicy: Always
status: {}

---

kind: Service
apiVersion: v1
metadata:
  name: upload-service
spec:
  selector:
    app: upload-service
  ports:
    - port: 80 # Default port for image